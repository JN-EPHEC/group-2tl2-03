include "/etc/bind/named.conf.options";
include "/etc/bind/named.conf.local";
logging {
    channel stderr_log {
        stderr;
        severity debug; # On met en debug pour tout voir
    };
    category default { stderr_log; };
    category config { stderr_log; };
};

